# tRPC-Go clickhouse 插件 examples

在本路径运行demo：`go build`

```shell
# 目前该demo支持事务回滚说明
./examples --help
Usage of ./examples:
  -op string
          select, selectrow, transaction. default: select (default "select")
```
