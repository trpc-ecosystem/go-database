# tRPC-Go clickhouse plugin examples

Run the demo in this path：`go build`

```shell
# Currently the demo supports transaction rollback instructions
./examples --help
Usage of ./examples:
  -op string
          select, selectrow, transaction. default: select (default "select")
```
