# tRPC-Go mysql plugins examples

Run the demo in this path:`go build`

```shell
# The demo currently supports transaction rollback instructions.
./examples --help
Usage of ./examples:
  -op string
          select, selectrow, transaction. default: select (default "select")
```
